using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testChildenfile : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("我是在热更文件的子文件夹中创建的脚本");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
